{
  "name": "notification-worker",
  "version": "1.0.0",
  "private": true,
  "description": "SQS-triggered Lambda worker: formats a welcome notification for a UserProfileCreated event and publishes it to SNS.",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "@aws-sdk/client-sns": "^3.600.0",
    "jest": "^29.7.0"
  }
}
